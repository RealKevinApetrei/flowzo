# Raw data — too large for git, obtain separately
data/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environments
.venv/
venv/
env/

# Model artefacts (raw formats only — serialized models in models/ are committed)
*.pkl
*.model
